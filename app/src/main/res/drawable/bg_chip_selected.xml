<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 테두리 -->
    <stroke android:width="1dp" android:color="@color/theme_orange" />

    <!-- 배경색 (알파 적용된 오렌지) -->
    <solid android:color="@color/theme_orange_alpha" />

    <!-- 둥근 모서리 -->
    <corners android:radius="24dp" />
</shape>
